{% extends "./base.njk" %}

{% block content %}
<main class="container mx-auto pt-36 pb-24">
    <section class="px-6">
        <h1 class="hidden">{{ title }}</h1>
        <div class="max-w-2xl text-base generated">
            {{ content | safe }}
        </div>
        <div class="py-3">
            <a href="/{{download_bio}}" class="text-black hover:text-gray_500 no-underline text-base sm:text-lg uppercase leading-none font-bold" download>Download CV</a>
        </div>
</main>{% endblock %}