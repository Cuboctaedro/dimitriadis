{% extends "./base.njk" %}

{% block content %}
    <main class="container mx-auto pt-36 pb-24">
        <ul class=" w-full ">
            {% for item in collections.booksSorted %}
                <li class="mb-24">
                    <article class="relative flex flex-row flex-wrap">
                        <div class="w-full sm:w-1/2 md:w-1/3 lg:w-1/4 px-6">
                            {% image item.data.cover, item.data.title + ' cover', 'md' %}
                        </div>
                        <div class="w-full sm:w-1/2 md:w-2/3 lg:w-3/4 pr-6">
                            <h1 class="font-semibold text-base sm:text-lg mb-4">{{ item.data.title }}</h1>
                            {% if(item.data.texts) %}
                            <div class="pb-1">
                                <span class="uppercase text-sm">Texts: </span>{{item.data.texts}}
                            </div>
                            {% endif %}
                            {% if(item.data.language) %}
                            <div class="pb-1">
                                <span class="uppercase text-sm">Language: </span>{{item.data.language}}
                            </div>
                            {% endif %}
                            {% if(item.data.editor) %}
                            <div class="pb-1">
                                <span class="uppercase text-sm">Editor: </span>{{item.data.editor}}
                            </div>
                            {% endif %}
                            {% if(item.data.design) %}
                            <div class="pb-1">
                                <span class="uppercase text-sm">Design: </span>{{item.data.design}}
                            </div>
                            {% endif %}
                            {% if(item.data.publisher) %}
                            <div class="pb-1">
                                <span class="uppercase text-sm">Publisher: </span>{{item.data.publisher}}
                            </div>
                            {% endif %}
                            {% if(item.data.description) %}
                            <div class="pb-1">
                                {{item.data.description}}
                            </div>
                            {% endif %}            
                            {% if(item.data.isbn) %}
                            <div class="pb-1">
                                {{item.data.isbn}}
                            </div>
                            {% endif %}
                        </div>
                    </article>
                        
                        
                </li>
            {% endfor %}
        </ul>
    </main>
{% endblock %}